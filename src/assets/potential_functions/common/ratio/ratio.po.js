import badge from "@asset/badges/sword.png";
import config from "./Ratio.ptcfg.vue";

export default {
    name: "ratio",
    chs: "Valid Entries",
    description: [
        "The maximum value of a single reinforcement of a valid entry is 1 point"
    ],
    badge,
    config,
}